# This rule is not used by the project kube-copilot-agent itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to kubecopilot.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kube-copilot-agent
    app.kubernetes.io/managed-by: kustomize
  name: kubecopilotmessage-viewer-role
rules:
- apiGroups:
  - kubecopilot.io
  resources:
  - kubecopilotmessages
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kubecopilot.io
  resources:
  - kubecopilotmessages/status
  verbs:
  - get
