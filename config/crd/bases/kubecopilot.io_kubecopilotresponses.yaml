---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubecopilotresponses.kubecopilot.io
spec:
  group: kubecopilot.io
  names:
    kind: KubeCopilotResponse
    listKind: KubeCopilotResponseList
    plural: kubecopilotresponses
    singular: kubecopilotresponse
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.agentRef
      name: AgentRef
      type: string
    - jsonPath: .spec.sessionID
      name: SessionID
      type: string
    - jsonPath: .spec.sendRef
      name: SendRef
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: KubeCopilotResponse is the Schema for the kubecopilotresponses
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              KubeCopilotResponseSpec defines the content of a KubeCopilotResponse.
              This object is immutable once created by the operator webhook.
            properties:
              agentRef:
                description: AgentRef is the name of the KubeCopilotAgent that produced
                  this response.
                type: string
              prompt:
                description: Prompt is the user message that triggered this response.
                type: string
              response:
                description: Response is the agent's reply.
                type: string
              sendRef:
                description: SendRef is the name of the KubeCopilotSend that initiated
                  this request.
                type: string
              sessionID:
                description: SessionID is the conversation session this response belongs
                  to.
                type: string
              usage:
                description: Usage contains token consumption and estimated cost for
                  this response.
                properties:
                  estimatedCost:
                    description: |-
                      EstimatedCost is the estimated USD cost for this response based on model pricing.
                      Stored as a string to preserve decimal precision (e.g. "0.002345").
                    type: string
                  inputTokens:
                    description: InputTokens is the number of tokens in the prompt
                      sent to the model.
                    format: int64
                    type: integer
                  model:
                    description: Model is the model identifier used for this response
                      (e.g. "gpt-4o").
                    type: string
                  outputTokens:
                    description: OutputTokens is the number of tokens in the model's
                      reply.
                    format: int64
                    type: integer
                  totalTokens:
                    description: TotalTokens is the sum of input and output tokens.
                    format: int64
                    type: integer
                type: object
            required:
            - agentRef
            - prompt
            - response
            type: object
          status:
            description: KubeCopilotResponseStatus defines the observed state of KubeCopilotResponse.
            properties:
              createdAt:
                description: CreatedAt is the timestamp when this response was recorded.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
