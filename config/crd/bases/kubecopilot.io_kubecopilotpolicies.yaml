---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubecopilotpolicies.kubecopilot.io
spec:
  group: kubecopilot.io
  names:
    kind: KubeCopilotPolicy
    listKind: KubeCopilotPolicyList
    plural: kubecopilotpolicies
    singular: kubecopilotpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.active
      name: Active
      type: boolean
    - jsonPath: .spec.agentRef
      name: AgentRef
      type: string
    - jsonPath: .status.ruleCount
      name: Rules
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          KubeCopilotPolicy is the Schema for the kubecopilotpolicies API.
          It defines guardrails and safety constraints for AI agent operations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeCopilotPolicySpec defines the desired state of KubeCopilotPolicy.
            properties:
              agentRef:
                description: |-
                  AgentRef optionally binds this policy to a specific KubeCopilotAgent.
                  When empty, the policy applies to all agents in the namespace.
                type: string
              rules:
                description: |-
                  Rules is the list of policy rules. Deny rules are always evaluated before
                  require-approval rules (deny takes precedence regardless of order).
                items:
                  description: PolicyRule defines a single guardrail rule evaluated
                    against agent send requests.
                  properties:
                    commandPatterns:
                      description: |-
                        CommandPatterns is a list of glob patterns matched against the send message.
                        A rule triggers if any pattern matches. Supports "*" and "?" wildcards.
                        Examples: "kubectl delete namespace prod-*", "* --force *"
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: set
                    message:
                      description: Message is the explanation shown to the user when
                        this rule triggers.
                      type: string
                    name:
                      description: Name is a human-readable identifier for this rule.
                      maxLength: 253
                      minLength: 1
                      type: string
                    type:
                      description: 'Type is the action taken when this rule matches:
                        "deny" or "require-approval".'
                      enum:
                      - deny
                      - require-approval
                      type: string
                  required:
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
            required:
            - rules
            type: object
          status:
            description: KubeCopilotPolicyStatus defines the observed state of KubeCopilotPolicy.
            properties:
              active:
                description: Active indicates whether this policy is currently being
                  enforced.
                type: boolean
              conditions:
                description: Conditions represent the current state of the policy.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              ruleCount:
                description: RuleCount is the number of rules in this policy.
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
