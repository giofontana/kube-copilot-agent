apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-copilot-agent

commonLabels:
  app: kube-copilot-ui

resources:
  - serviceaccount.yaml
  - rbac.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml

images:
  - name: quay.io/gfontana/kube-copilot-agent-ui
    newTag: v1.0
